# ZeroPayModule — required environment variables
#
# Web Push (VAPID) — generate keys with:
#   vendor/bin/web-push generate-vapid-keys
# or https://vapidkeys.com
#
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@zeropay.io
