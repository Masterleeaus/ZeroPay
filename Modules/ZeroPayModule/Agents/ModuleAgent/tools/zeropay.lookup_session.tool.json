{
  "name": "zeropay.lookup_session",
  "description": "Look up a ZeroPay payment session by session token or ID, scoped to the current company",
  "type": "query",
  "action": "Modules\\ZeroPayModule\\Actions\\LookupZeroPaySessionAction",
  "requires_permission": "zeropay.view",
  "tenant_scoped": true,
  "input_schema": {
    "type": "object",
    "properties": {
      "session_id": {
        "type": "string",
        "description": "Session token or numeric ID of the payment session"
      }
    },
    "required": ["session_id"]
  }
}
