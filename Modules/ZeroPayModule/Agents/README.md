# Module Agents

Users always talk to **Zero**.

This folder contains internal module-specialist agents that Zero can route to. The included `DemoAgent` is a scaffold only and must be replaced before production.
